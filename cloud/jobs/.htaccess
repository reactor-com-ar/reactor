# Los scripts de esta carpeta son CLI puros, disparados por el scheduler
# del monorepo. NUNCA deben servirse por HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
